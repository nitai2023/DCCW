import { Box, Typography } from '@mui/material';

export function Loading() {
  return (
    <Box>
      <Typography variant="h1" color={'primary'}>
        Loading...
      </Typography>
    </Box>
  );
}
