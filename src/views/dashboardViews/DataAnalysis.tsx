import { Box, Typography } from '@mui/material';
export function DataAnalysis() {
  return (
    <Box>
      <Typography variant="h3">数据分析</Typography>
      <Typography variant="body1">这里可以展示一些数据分析图表</Typography>
    </Box>
  );
}
